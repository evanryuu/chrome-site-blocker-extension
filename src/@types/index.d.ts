export interface IUrl {
  url: string
}

export interface ICountdown {
  days: number,
  hours: number,
  minutes: number,
  seconds: number,
  milliseconds: number,
  totalDays: number,
  totalHours: number,
  totalMinutes: number,
  totalSeconds: number,
  totalMilliseconds: number
}
